= content_for(:page_header) do
  New Hotel

= simple_form_for resource do |hotel|
  = render partial: 'form', locals: { hotel: hotel }
  .actions
    = hotel.resource_submit
    = link_to 'Cancel', hotels_path, :class => 'btn'